<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineLearningApp"
        tools:targetApi="31">
        <activity
            android:name=".Activity.HomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.LoginActivity" />
        <activity android:name=".Activity.RegisterActivity" />
        <activity android:name=".Activity.ResetPasswordActivity" />
        <activity android:name=".Activity.UserProfileActivity" />
        <activity android:name=".Activity.ChangeProfileActivity" />
        <activity android:name=".Activity.CourseListActivity" />
        <activity android:name=".Activity.MyCoursesActivity" />
        <activity android:name=".Activity.CourseDetailsActivity" />
        <activity android:name=".Activity.LessonDetailsActivity" />
        <activity android:name=".Activity.TakeQuizActivity" />
        <activity android:name=".Activity.QuizResultActivity" />
    </application>

</manifest>